<UserControl x:Class="CrashLens.App.DetailSection" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <StackPanel Spacing="7"><TextBlock Text="{x:Bind Title, Mode=OneWay}" Foreground="{StaticResource MutedBrush}" FontSize="12" FontWeight="SemiBold"/><ItemsControl ItemsSource="{x:Bind Items, Mode=OneWay}"><ItemsControl.ItemTemplate><DataTemplate><Grid ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Key}" Foreground="{StaticResource MutedBrush}"/><TextBlock Grid.Column="1" Text="{Binding Value}" FontFamily="Cascadia Mono" TextWrapping="Wrap"/></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel>
</UserControl>
